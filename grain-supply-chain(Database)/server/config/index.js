//Houyuan
const serverConfig = require('./module/server.config');
const mongoConfig = require('./module/mongo.config');
const bcryptConfig = require('./module/bcrypt.config');

module.exports = {
    server: serverConfig,
    mongo: mongoConfig,
    bcrypt: bcryptConfig
}//Houyuan